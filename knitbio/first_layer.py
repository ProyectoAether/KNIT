

def knit(
        sparql_service: str,
        REST_URL: str,
        list_text: list,
        API_KEY: str,
        IP_SERVER_NEO4J: str,
        USER_NEO4J: str,
        PASSWORD_NEO4J: str,
        wc: str,
        wa: str,
        wd: str,
        ws: str,
        ONTOLOGY: list,
        ontology_denied=list
):
    